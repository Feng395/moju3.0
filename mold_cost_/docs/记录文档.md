本轮继续把 CAD split 的 legacy 主入口真正切到了 `src` 侧 orchestration。

这次没有再新增一层 runtime，而是直接让 `scripts/cad_chaitu/main.py` 改为委托现有的 `src/mold_cost/infrastructure/cad/cad_process_runtime.py`。现在 `chaitu_process()` 不再自己串联 DWG 来源解析、输入准备、子图识别/导出、板料线、上传、`.x_t` 导出与落库统计，而是只负责 legacy 管理器初始化与参数注入；真正的主流程编排已经统一收口到 `execute_cad_split_process()`。

这一步的意义比较直接：R6 里“CAD 主流程仍在 legacy”这一层已经进一步收缩，后面需要继续啃的就更聚焦在 `analysis_system` / `analyzer` / `MaterialLineIntegrator` 这些算法和实现本体，而不是继续维护一个超长的 `main.py`。也就是说，CAD 方向后续的刀法可以更集中在算法边界，而不是胶水编排。

本轮验证结果：

`pytest mold_cost_/tests/unit/test_cad_process_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py mold_cost_/tests/unit/test_cad_analysis_runtime.py mold_cost_/tests/unit/test_cad_material_line_runtime.py mold_cost_/tests/unit/test_cad_upload_runtime.py mold_cost_/tests/unit/test_cad_split_persistence_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py -q`

结果：`16 passed`

`pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q`

结果：`188 passed`
本轮继续往 review 默认识别链里收尾，重点不是再碰 handler，而是缩小 `agents.intent_recognizer` 的 fallback 面积。

这次更新了 `src/mold_cost/infrastructure/review/intent_recognizer_runtime.py`。除了原本已有的简单执行型意图、验证类查询和常见 `query_type` 规则外，现在又补了两类 src-first 识别：一类是“刚才/这个零件/那个零件/按刚才那套”这类上下文指代消息，可以直接落成 `QUERY_DETAILS` 或 `DATA_MODIFICATION`，不再默认回退 legacy；另一类是标准费用/时间查询里的稳定 `query_type`，新增覆盖了 `wire_base`、`add_auto_material`、`nc_base`、`standard` 等类型。

这样一来，review 默认链里真正还需要依赖 `agents.intent_recognizer` 的部分就又缩小了一截，主要收敛到更复杂、更模糊、需要 LLM 或深上下文推断的 query / modification 消息。也就是说，R3 现在离“默认链只剩兼容壳”又近了一步。

本轮验证结果：

`pytest mold_cost_/tests/unit/test_review_workflow_adapters.py -q`

结果：`28 passed`

`pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q`

结果：`191 passed`

本轮继续把 feature 链上最后一块“`.x_t` 下载 + NX 红色面提取 + metadata 写回”的滑块辅助能力收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/slider_red_face_update_runtime.py`，把原来 `scripts/feature_recognition/slider_red_face_updater.py` 里的三段核心逻辑统一迁到了 `src`：一是兼容 `download_file/get_file` 的 `.x_t` 获取；二是 NXOpen 红色面面积/数量提取；三是把结果写回 `features.metadata.wire_cut_details`。同时，`legacy_feature_recognition_gateway.py` 的 `batch_recognize()` 现在已经直接注入这个 src-owned runtime，不再从 legacy 脚本导入更新函数；原 `slider_red_face_updater.py` 也已经退化为兼容壳，只负责桥接到 `src`。

这样一来，feature 方向当前留在 legacy 的滑块后处理基本已经收口完成。后面 R6 的重心就可以更明确地放回 CAD split 主流程本体，也就是 `scripts.cad_chaitu.main.chaitu_process` 以及 analyzer / export / material line 这些尚未完全迁出的实现，而不是继续在 feature 辅助脚本里打转。

本轮验证结果：

`pytest mold_cost_/tests/unit/test_slider_red_face_update_runtime.py mold_cost_/tests/unit/test_feature_batch_runtime.py mold_cost_/tests/unit/test_feature_refactor.py -q`

结果：`13 passed`

`pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q`

结果：`185 passed`

结合文档和当前代码，我的判断是：项目现在处于“新架构已接管编排与边界，但核心业务实现仍大量桥接旧系统”的阶段。不是早期试验态了，也还不是“重构完成态”。更准确地说，是“整合中后期，切换未完成”。

文档末尾的总体结论和代码大体一致，尤其阶段 20-26 基本都能在代码里找到实锤；但对剩余 legacy 范围的描述略偏乐观，实际残留不只在 review/pricing calculator，还包括 API 外壳和部分 application use case 对 api_gateway 的直接依赖。

关键判断

工作流编排已经真正落到 LangGraph，不只是占位。job_graph.py (line 18) 在 job_graph.py (line 187) 编译 StateGraph，并接入了本地 durable checkpoint store；job_file_checkpoint_store.py (line 11) 负责落盘。对应的恢复链路在 test_job_graph_checkpoint_resume.py (line 14) 和 test_job_graph_checkpoint_resume.py (line 68) 也有验证。
但 job_graph 目前接管的是“流程控制”，不是“执行逻辑本体”。真正开始/继续执行时，仍然回调旧 orchestrator agent：job_graph.py (line 371) 和 job_graph.py (line 721)。
Review 链也已经是 LangGraph 真运行，包含 interrupt/resume。review_graph.py (line 364)、review_graph.py (line 609)、review_graph.py (line 675) 都说明这一点。
但 Review 默认装配仍有明显 legacy 痕迹：默认 session/state 还是 Redis，review_graph.py (line 394)；数据加载和修改应用仍走 legacy review repository/handler，review_data_loader.py (line 13)、review_change_applier.py (line 19)。
基础设施收口基本成立。统一配置、DB、MinIO、RabbitMQ 入口都已存在：settings.py (line 13)、session.py (line 12)、minio_client.py (line 21)、rabbitmq_client.py (line 19)。
API/worker 已开始统一走 use case/workflow。旧任务服务已变成兼容外壳：job_service.py (line 1)；continue 统一发 thread_id=job_id 消息并必要时回退本地 workflow：continue_job.py (line 15)；旧 worker 也只负责把消息交给 workflow：orchestrator_worker.py (line 31)。
但接口层还没有真正“翻面”。src/mold_cost/interfaces/api 目前主要还是对旧 api_gateway 的包裹：app.py (line 8)、jobs.py (line 8)。
Application 层也还没完全摆脱旧仓储，几个 use case 仍直接 import api_gateway.repositories/utils：create_job.py (line 13)、get_job.py (line 10)、get_job_file.py (line 10)。这一点说明“依赖方向完全理顺”还没结束。
CAD/feature 已经形成了 service + gateway 的稳定契约，但算法本体仍在脚本层。split_service.py (line 17)、legacy_cad_split_gateway.py (line 12)、recognition_service.py (line 17)、legacy_feature_recognition_gateway.py (line 12)。
Pricing 迁移不均衡，search 明显快于 calculator。像 base_itemcode_search.py (line 36) 已接到新的 snapshot search service，price_snapshot_search_service.py (line 33)。但 calculator 基本还是桥接，我统计到 domain/pricing/calculators 里 24 个实现文件全部是 import_module("scripts.calculate.*") 形态，price_total.py (line 1) 就是典型。pricing_service 和 process_rule_matcher 也仍挂旧 agent/script：pricing_service.py (line 13)、process_rule_matcher.py (line 5)。
实际状态归纳

如果按层次看：

骨架与基础设施：已基本成形。
Workflow 编排：已进入后期，Job/Review 两条主链都真实跑在 LangGraph 上。
领域实现迁移：中期，CAD/feature 是“新契约+旧算法”，pricing search 部分落地，pricing calculator 仍大面积未迁。
接口替换：中期偏后，src/interfaces 还没真正成为独立主入口。
我还实际跑了回归：pytest tests/unit tests/integration tests/golden -q，结果是 32 passed in 1.43s，与文档 REFACTOR_PROGRESS.md (line 828) 一致。
不过 golden 样本目前仍偏薄，我只看到 1 组样本目录：tests/golden/samples。

下一步优先级我会给三件事：先把 application/use_cases 和 interfaces/api 从 api_gateway 脱钩，再完成 pricing 的 search/total_search/wire_total_search -> calculator/process matcher 迁移，最后替换 review 的 legacy repository/Redis 默认装配。
如果你要，我下一步可以直接把这份分析整理成一张“已完成 / 半完成 / 未完成 / 风险点”的对照表。

将现有修改提交至git 填写详细的中文提交信息 然后写一个剩余阶段任务对照表 再调用子agent完成剩余的重构任务



已处理 19m 9s
已完成，当前工作区已清空。

本轮一共落了 4 个提交：

ec149e5 feat(refactor): 落地 durable checkpoint、review 去 agent 化与第二批 pricing search 迁移
5d42d7f refactor(use-case): 下沉 application/use_cases 依赖到 infrastructure 适配层
32f3388 refactor(api): 让 src 接管 API 入口并补齐 jobs/files 兼容层
d864836 refactor(review): 收口 review repository 适配并惰性化 MinIO 初始化
剩余阶段任务对照表已经写到 REFACTOR_REMAINING_TASK_TABLE.md。
我也按这张表调用了 3 个子 agent，并把结果集成到了主工作区：

R1 已完成：application/use_cases 不再直接依赖 api_gateway.repositories/utils，改走 src/mold_cost/infrastructure 适配层。关键文件在 create_job.py、get_job.py、get_job_file.py。
R2 已完成当前波次：src 下 API 入口已真实接管，旧 api_gateway 反向包装新入口。关键文件在 app.py、jobs.py、files.py、api_gateway/main.py。
R3 已完成当前波次：review 默认仓储访问已收口到新适配层，不再直接 import api_gateway.repositories.review_repository。关键文件在 review_repository_adapter.py、review_data_loader.py、review_change_applier.py、review_graph.py。
另外我补了一个必要修正：把 minio_client.py 改成惰性 bucket 初始化，避免 API 导入阶段就外连 MinIO。
验证结果：

pytest tests/unit/test_api_interface_entrypoints.py tests/unit/test_review_workflow_adapters.py tests/unit/test_use_case_infra_adapters.py -q
pytest tests/unit tests/integration tests/golden -q
结果：42 passed
还没完成的主要剩余项是：

pricing 第三批迁移：search.py、total_search.py、wire_total_search.py 以及后续 calculator / process matcher
review 里对 agents.action_handlers、agents.confirm_handler 的 legacy handler 依赖
checkpoint backend 从本地 fallback 升级到可共享 durable 存储
CAD / feature 算法本体从 scripts/* 继续迁出
如果你要，我下一步可以继续按 REFACTOR_REMAINING_TASK_TABLE.md 的 Wave B 直接推进 pricing 主链迁移。

继续推进了一波，当前工作区已清空，最新提交是 4fc9b0f refactor(pricing): 落地第三批 search 真实实现并迁移 process matcher。

这轮实际完成了三件事。第一，pricing 第三批 search 已经落地，search.py、total_search.py、wire_total_search.py 不再桥接 scripts.search.*，现在走真实 domain 实现和仓储查询能力，相关代码在 search.py、total_search.py、wire_total_search.py、price_snapshot_search_service.py、pricing_snapshot_repository.py。第二，process_rule_matcher 已从脚本桥接迁到 src/mold_cost，见 process_rule_matcher.py。第三，文档已同步，见 REFACTOR_PROGRESS.md 和 REFACTOR_REMAINING_TASK_TABLE.md。

回归结果是 pytest tests/unit tests/integration tests/golden -q => 46 passed。新增了 test_pricing_process_rule_matcher.py，并扩展了 test_pricing_search_refactor.py 和 pricing_bridge_inventory.json。

重构还没有完成，剩余核心已经进一步收敛到两块：

pricing calculators 主链，优先是 price_material.py、price_wire_total.py、price_total.py
review handler 仍有对 legacy agents.* 的依赖
如果按当前节奏继续，下一波我会直接进入 price_material -> price_wire_total -> price_total。

本轮继续推进并且已经提交，当前工作区是干净的。

最新两笔提交：

4cfdcf4 refactor(pricing): 将 weight calculator 迁移为 domain 实现
42fb989 refactor(pricing): 落地第二批 calculator 真实实现并推进 inventory
这两笔把 price_weight、price_heat、price_nc_total 从 legacy bridge 迁成了真实 domain 实现，并同步更新了 REFACTOR_PROGRESS.md、REFACTOR_REMAINING_TASK_TABLE.md 和 pricing_bridge_inventory.json。现在 pricing 已迁出的 calculator 包括：

price_material
price_wire_total
price_total
price_weight
price_heat
price_nc_total
回归结果已经更新到 pytest tests/unit tests/integration tests/golden -q => 63 passed。

我已经起了下一批并行任务，目标是：

price_nc_base
price_tooth_hole
price_water_mill_total
下一轮会直接接这三块继续往前推。

重构还没有“全项目完成”。当前剩余高优先级已经收缩到：

pricing_service.py 里的 calculate -> PricingAgent 兼容桥接
pricing_agent_local.py 的兼容编排外壳
server.py 里的 pricing 兼容路由
review / durable backend / CAD-feature 算法本体这些非-pricing 残项
如果继续下一步，我会直接开始拆 pricing_service.calculate，把 pricing 主链最后的 agent 运行时桥接拿掉。

继续推进了一轮，重点落在 feature recognition。

这次我把 DXF 单文件分析入口从 legacy 大脚本里剥出来了。新增 feature_analysis_runtime.py，现在 legacy_feature_recognition_gateway.py 的 analyze_dxf() 已经直接走 src/mold_cost 里的 runtime，而不是再从 scripts/feature_recognition/feature_recognition.py 取 analyze_dxf_features。同时我把 scripts/feature_recognition/init.py 改成了惰性导出，避免包初始化时就把重型 legacy 脚本整包拉起。

测试也补上了：test_feature_analysis_runtime.py 锁住新 runtime 的最小输出契约，test_feature_refactor.py 补了 gateway 走新 runtime 的回归。全量结果现在是 pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q -> 132 passed。文档也同步到了 REFACTOR_PROGRESS.md 和 REFACTOR_REMAINING_TASK_TABLE.md。

重构还没完成，剩余主项现在收敛到：

feature 里的 batch_feature_recognition_process、get_subgraphs_from_db、save_features_to_db
CAD 拆图主流程
review 修改确认链底层 legacy handler
真正跨实例共享的 durable backend
下一轮我会继续拆 feature 批处理/落库链。


本轮继续推进了 3 块，而且都已经过回归。

feature 批处理编排已经迁入 feature_batch_runtime.py，legacy_feature_recognition_gateway.py 现在走 src-owned batch/runtime，而不是直接回到旧脚本入口。
review 确认执行链已经从 agents.confirm_handler 摘掉。新的默认确认执行器在 confirmation_executor.py，配套的 pending action 存储在 pending_action_store.py，默认装配仍由 legacy_review_handler_adapter.py 负责。
CAD split 这边先立起了新的 runtime 边界：cad_split_runtime.py。同时 legacy_cad_split_gateway.py 和 cad_pool.py 都改成了更干净的 src-owned / lazy bridge 形态。
文档也同步到了当前状态，并且再次按 UTF-8 重写验证过：REFACTOR_PROGRESS.md 和 REFACTOR_REMAINING_TASK_TABLE.md。

验证结果：

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
145 passed
重构还没有全完成，剩余核心已经进一步收敛到：

review 的 intent recognizer / action handler registry 仍经由 legacy adapter
durable backend 仍是共享文件型实现，还不是跨实例 shared backend
feature 的 get_subgraphs_from_db / save_features_to_db
CAD 的 scripts.cad_chaitu.main.chaitu_process 主流程本体
如果继续下一轮，我会直接先拆 review 剩余的 recognizer / action handler 链。


本轮继续推进了 review handler runtime 这一层，并且保持了可单独提交的收口范围。

这次新增了 `src/mold_cost/infrastructure/review/action_handler_runtime.py`，把 review 默认链上的 handler 注册表和启动预热从 `agents.action_handlers.base_handler.ActionHandlerFactory` 收回到 `src`。现在 `interfaces/api/app.py` 启动时会直接初始化 src-owned runtime，`legacy_review_handler_adapter.py` 只负责 legacy recognizer 和默认 change applier 的装配，不再负责 handler factory。

另外，`FEATURE_RECOGNITION`、`PRICE_CALCULATION`、`WEIGHT_PRICE_CALCULATION` 这三类“准备 pending action -> 等用户确认”的 handler 也已经接到了 `src/mold_cost/infrastructure/review/review_action_handlers.py`。这一步不是只换注册表，而是把 review 默认链里最适合先下沉的一批 handler 真正迁出了旧工厂。

这意味着 review 这条主链又缩短了一层 legacy 依赖：`ConfirmHandler` 和 `ActionHandlerFactory` 都已经摘掉，剩余真正还没迁出的 review runtime 主要是 `agents.intent_recognizer`，以及 `data_modification/query_details/general_chat/weight_price_query` 这几类复杂 handler。下一轮更合理的切入点就是继续把 recognizer 与这些 concrete handlers 逐个下沉。

本轮补的验证主要是：

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py mold_cost_/tests/unit/test_api_interface_entrypoints.py -q

全量回归结果已经更新为：

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
145 passed


本轮继续推进了 review 剩余查询链，优先挑了边界更清晰的 `WEIGHT_PRICE_QUERY` 下手。

这次新增了 `src/mold_cost/infrastructure/review/weight_price_query_handler.py`，把“按重量计算详情查询”从 legacy handler 独立到了 `src` 侧 review runtime。新的实现默认复用 `src/mold_cost/infrastructure/db/repositories/chat_history_repository.py` 提供的历史会话适配接口，可以在用户只说“它/这个”的情况下，通过最近聊天记录回推目标子图，然后直接查询 `ProcessingCostCalculationDetail.weight_price_steps` 并返回格式化结果。

为配合这一步，`chat_history_repository.py` 补上了 `get_session_history()` 和 `get_recent_session_history()` 两个适配方法，review 的 `action_handler_runtime.py` 也已经把 `WEIGHT_PRICE_QUERY` 正式切到 `src`，不再落回 `agents.action_handlers.weight_price_query_handler`。这样 review 默认链剩下的 legacy concrete handler 就进一步缩小到了 `data_modification` 和 `query_details` 两块，复杂 fallback 也主要收敛在 `agents.intent_recognizer` 里。

本轮验证结果：

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py -q
18 passed

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py mold_cost_/tests/unit/test_api_interface_entrypoints.py mold_cost_/tests/integration/test_review_graph_checkpoint_resume.py -q
23 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
151 passed

重构还没有全完成。当前 review 方向下一步更合适的切入点，是继续把 `QUERY_DETAILS` 与 `DATA_MODIFICATION` 从 legacy handler 下沉到 `src`，再顺手压缩复杂 query / modification 的 recognizer fallback。


本轮继续把 review 的只读查询链再往前推了一步，目标是把 `QUERY_DETAILS` 也从 legacy handler 收回来。

这次新增了 `src/mold_cost/infrastructure/review/query_details_review_handler.py`，现在默认 review action runtime 已经把 `QUERY_DETAILS` 映射到 `src` 侧 handler，不再落回 `agents.action_handlers.query_details_handler`。新的实现保留了几项核心行为：一是继续通过聊天历史回推子图 ID，处理“它/那个/这个”这类追问；二是继续查询 `ProcessingCostCalculationDetail` 并补带 `Feature.processing_instructions`；三是保留 `query_type` 过滤与规则格式化，支持 `material` 这类单类查询，也支持 `nc / wire / water_mill` 这类聚合分类查询。

这样做之后，review 默认链里真正还挂在 legacy concrete handler 上的部分就只剩 `DATA_MODIFICATION` 了；剩余更大的 legacy 依赖面主要集中在 `agents.intent_recognizer` 的复杂 query / modification fallback。也就是说，review 这条链的下一轮目标已经更明确了，就是把修改链和复杂意图识别一起收口。

本轮验证结果：

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py -q
20 passed

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py mold_cost_/tests/unit/test_api_interface_entrypoints.py mold_cost_/tests/integration/test_review_graph_checkpoint_resume.py -q
25 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
153 passed


本轮继续把 review 最后一块 concrete handler 也迁进了 `src`，也就是 `DATA_MODIFICATION`。

这次新增了 `src/mold_cost/infrastructure/review/data_modification_review_handler.py`，默认 review action runtime 现在已经把 `DATA_MODIFICATION` 也映射到 `src` 侧实现，不再落回 `agents.action_handlers.data_modification_handler`。新的实现继续复用既有的 `NLPParser`、`ModificationValidator` 和 `DataViewBuilder`，同时把 pending action 保存统一走 `src` 侧存储适配。针对“材质改为 XXX”这种没有明确点名子图、但 parser 又解析出了多条候选修改的情况，新的 handler 会优先用最近历史消息里的子图 ID 规则缩小目标范围，再进入校验和修改应用。

到这一步，review 默认链上的 concrete handlers 已经全部迁到 `src/mold_cost`：`DATA_MODIFICATION`、`FEATURE_RECOGNITION`、`PRICE_CALCULATION`、`QUERY_DETAILS`、`WEIGHT_PRICE_CALCULATION`、`WEIGHT_PRICE_QUERY`、`GENERAL_CHAT` 都不再依赖 legacy handler 工厂或 legacy handler 实例。review 方向剩下的主要 legacy 负担，已经收敛到 `agents.intent_recognizer` 里的复杂 query / modification fallback 分支。

本轮验证结果：

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py -q
22 passed

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py mold_cost_/tests/unit/test_api_interface_entrypoints.py mold_cost_/tests/integration/test_review_graph_checkpoint_resume.py -q
27 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
155 passed


本轮继续收 review 的识别阶段，把原来还会默认回落到 legacy 的常见 query / modification 规则，继续搬进了 `src` 侧 recognizer。

这次主要更新的是 `src/mold_cost/infrastructure/review/intent_recognizer_runtime.py`。除了之前的执行型意图和 general chat 规则外，现在 `src-first` recognizer 还能在本地直接识别几类高频消息：一是验证型问题（如“这样对吗”）会直接命中 `QUERY_DETAILS`；二是常见详情查询会直接抽出 `subgraph_id` 和 `query_type`；三是 `按重量怎么算` 这类消息会直接识别为 `WEIGHT_PRICE_QUERY`；四是典型修改表达（如“材质改为 S136”）会直接识别为 `DATA_MODIFICATION`。另外还补了一个小但很关键的规则：单个字母工艺码（L/W/M/G/K/Z）不再被误当成子图 ID，会保留给后续 handler 用历史消息推断真正目标。

到这一步，review 方向剩余的 legacy 识别依赖已经进一步收敛了。默认 recognizer 现在只会把更复杂、更模糊、需要 legacy LLM prompt 或特殊上下文修正的 query / modification 消息再交给 `agents.intent_recognizer`。也就是说，主链上最常见、最稳定的 review 交互路径已经都能在 `src` 内完成。

本轮验证结果：

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py -q
26 passed

pytest mold_cost_/tests/unit/test_review_workflow_adapters.py mold_cost_/tests/unit/test_api_interface_entrypoints.py mold_cost_/tests/integration/test_review_graph_checkpoint_resume.py -q
31 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
159 passed


本轮继续把 feature 的 DB helper 也收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/feature_persistence_runtime.py`，把原来还留在 `scripts/feature_recognition/feature_recognition.py` 里的 `get_subgraphs_from_db()` 和 `save_features_to_db()` 迁到了 `src` 侧 runtime。现在 `legacy_feature_recognition_gateway.py` 的 `get_subgraphs()` / `save_features()` 都已经直接走这个新的 persistence runtime，不再通过 legacy 大脚本取 DB helper。新的实现保留了原有关键行为：一是继续探测 `subgraphs.xt_file_url` 是否存在，兼容不同库表版本；二是继续 upsert `features`；三是继续初始化 `processing_cost_calculation_details`；四是继续回写 `subgraphs.wire_process_note / wire_process`；五是继续通过注入方式复用滑块红色面查表逻辑，而不是把 MinIO/红色面规则硬编码回 gateway。

这样一来，feature 方向剩余的 legacy 依赖就进一步缩小了。主链上真正还没迁出的，主要是 `slider_red_face_updater`、`slider_red_face_lookup` 这类滑块辅助能力；更大的 legacy 面已经转移到 CAD split 主流程，也就是 `scripts.cad_chaitu.main.chaitu_process` 和相关 storage / db / converter 运行时桥接。这也意味着下一轮如果继续按收口收益排序，最合理的目标已经从 “feature DB helper” 切到了 “CAD split 主流程本体”。

本轮验证结果：

pytest mold_cost_/tests/unit/test_feature_persistence_runtime.py mold_cost_/tests/unit/test_feature_analysis_runtime.py mold_cost_/tests/unit/test_feature_batch_runtime.py mold_cost_/tests/unit/test_feature_refactor.py -q
14 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
162 passed


本轮继续把 feature 链上最后一块“读 MinIO 查表”的轻量 helper 也收进了 `src`，目标是把 `slider_red_face_lookup` 从 legacy 脚本里摘掉。

这次新增了 `src/mold_cost/infrastructure/cad/slider_red_face_lookup_runtime.py`，把原来 `scripts/feature_recognition/slider_red_face_lookup.py` 里的两类核心能力迁到了 `src`：一类是从 MinIO 下载 `feature_database.json` 并做进程内缓存，另一类是根据 `part_code` 把滑块红色面数据补进 `wire_cut_details`。同时，`legacy_feature_recognition_gateway.py` 的 `save_features()` 和 `upload_feature_database()` 也已经切到 `src` 侧 runtime，不再为了红面查表和缓存失效回到 legacy lookup 脚本。这样一来，feature 方向当前真正在 legacy 里还没迁出的滑块辅助能力，就只剩 `slider_red_face_updater` 这一块。

这一步的收益不只是少一个脚本依赖，更关键的是把“持久化前查表补数据”和“上传新数据库后的缓存失效”都收口到了同一个 src runtime 里。后面如果我们要继续压 feature 链，这里已经是稳定的扩展点，不需要再穿过 legacy 包路径找 helper。

本轮验证结果：

pytest mold_cost_/tests/unit/test_slider_red_face_lookup_runtime.py mold_cost_/tests/unit/test_feature_persistence_runtime.py mold_cost_/tests/unit/test_feature_analysis_runtime.py mold_cost_/tests/unit/test_feature_batch_runtime.py mold_cost_/tests/unit/test_feature_refactor.py -q
16 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
164 passed


本轮继续推进了 CAD split 主流程，但我没有直接硬拆 `chaitu_process` 整体，而是先把两段最独立、最容易验证的后半段运行时收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/cad_split_persistence_runtime.py` 和 `src/mold_cost/infrastructure/cad/cad_xt_export_runtime.py`。前者负责“根据上传结果过滤可入库子图、调用 `db_manager.save_subgraph()`、汇总 `result_files / db_success_count / failed_upload_count / failed_db_count`”；后者负责“.x_t 导出前的 PRT 路径查询、NX 环境探测、PRT 下载以及 xt MinIO 基础路径拼装”。这样 `scripts/cad_chaitu/main.py` 里原来最容易膨胀的两段后处理逻辑，已经收口成了两个 src-owned runtime，而 legacy 脚本保留的就更接近真正难拆的主编排与算法本体。

这一步的好处是把 CAD split 的后半程职责分得更清楚了：上传成功后的落库统计现在不再散落在 `main.py` 里，`.x_t` 导出也不再混杂在长函数中间。后面如果我们继续拆 `storage`、`converter` 或 analyzer 主流程，这两块就不用再反复碰，风险会小很多。

本轮验证结果：

pytest mold_cost_/tests/unit/test_cad_split_refactor.py mold_cost_/tests/unit/test_cad_split_persistence_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py -q
7 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
168 passed


本轮继续顺着 CAD split 的后半程往前推了一步，把 `dwg/prt` 输入源解析也收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/cad_source_runtime.py`，把原来散落在 `scripts/cad_chaitu/main.py` 和 `.x_t` 导出准备里的两类判断统一成了 runtime：一类是 `is_probable_minio_object_path()` 这种对象路径识别；另一类是 `resolve_dwg_source()` / `resolve_prt_source()` 这种“必要时回查数据库，再决定是否走 MinIO”的输入源解析。随后 `main.py` 和 `cad_xt_export_runtime.py` 都改成了复用这套公共 runtime，不再各自维护一份近似规则。

这一刀虽然看起来不像“拆主流程”那么显眼，但收益其实很直接：CAD 主链里关于来源判定的语义现在只剩一份定义，后面如果我们继续往 `storage` 或 `converter` 层拆，就不会同时修改两三处重复逻辑，也不容易出现 `dwg` 和 `prt` 判定规则不一致的问题。

本轮验证结果：

pytest mold_cost_/tests/unit/test_cad_source_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py mold_cost_/tests/unit/test_cad_split_persistence_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py -q
10 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
171 passed


本轮继续把 CAD split 主流程里最前面的“输入文件准备阶段”也收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/cad_prepare_runtime.py`，把 `scripts/cad_chaitu/main.py` 里原本直接写在一起的三件事拆成了独立 runtime：构造 `input.dwg / input.dxf` 临时路径、通过 `storage_manager.get_file()` 获取源 DWG、再调用 `DWGConverter` 转成 DXF。现在 `main.py` 只负责把 `dwg_source / use_minio / temp_dir` 等上下文传给这个 runtime，再根据返回结果决定是否继续往后执行。

这一步的意义主要是把 CAD 主链最稳定的一段准备流程也从长函数里挪开。到这里为止，CAD split 的“来源解析”“输入准备”“`.x_t` 导出准备”“上传后持久化”都已经有了各自的 src-owned runtime；后面继续拆 `chaitu_process` 时，剩下的注意力就可以更集中地放在 analyzer/export/material line 这些真正复杂的业务编排上。

本轮验证结果：

pytest mold_cost_/tests/unit/test_cad_prepare_runtime.py mold_cost_/tests/unit/test_cad_source_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py mold_cost_/tests/unit/test_cad_split_persistence_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py -q
13 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
174 passed


本轮继续把 CAD split 中间那段“子图编号解析 + 导出计划整理”也收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/cad_region_runtime.py`，把原来写在 `scripts/cad_chaitu/main.py` 里的两段逻辑拆出来了：一段是对子图做 `resolve_region_info()` 后，再统一补默认 `part_name/part_code`、处理重复 `sub_code` 后缀；另一段是把这些识别结果转成 batch export 参数，以及把导出结果整理成后续上传/持久化阶段需要的 `export_files` 结构。这样 `main.py` 里最容易膨胀的“编号编排”和“导出结果归一化”部分就不再和真正的 analyzer/export/material-line 主流程混在一起。

到这一步，CAD split 主链已经有了比较清楚的一组 src-owned runtime：来源解析、输入准备、子图编号编排、`.x_t` 导出准备、上传后持久化。后面再继续拆的时候，真正需要啃的就更集中在 analyzer 本体、material line 集成和剩余主编排，而不是一些反复出现的列表整理和命名规则。

本轮验证结果：

pytest mold_cost_/tests/unit/test_cad_region_runtime.py mold_cost_/tests/unit/test_cad_prepare_runtime.py mold_cost_/tests/unit/test_cad_source_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py mold_cost_/tests/unit/test_cad_split_persistence_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py -q
15 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
176 passed


本轮继续把 CAD split 后半程再往前压了一截，重点落在板料线后处理和上传这两块。

这次新增了 `src/mold_cost/infrastructure/cad/cad_material_line_runtime.py` 和 `src/mold_cost/infrastructure/cad/cad_upload_runtime.py`。前者把 `main.py` 里那段“读取环境变量判断是否启用板料线、遍历导出子图、解析 L/W/T、调用 `MaterialLineIntegrator`、可选保存 debug 副本”的逻辑统一收口到了 `src`；后者则把上传前的 `upload_list` 组装和 `batch_upload_files()` 调用也抽成了一个很薄的 runtime。这样 `scripts/cad_chaitu/main.py` 里后半段又少掉了一批和主算法无关的列表整理与开关判断。

到这一步，CAD split 方向剩余的真正 legacy 核心已经越来越集中：现在更主要的未迁出部分，已经是 `analysis_system.analyzer.analyze_cad_file_streaming()`、`resolve_region_info()`、`batch_export_regions_concurrent()` 这些 analyzer/export 主流程本体，以及 feature 侧还没动的 `slider_red_face_updater`。也就是说，外围稳定编排层已经基本搭起来了，接下来要啃的会更偏算法和工作流本体。

本轮验证结果：

pytest mold_cost_/tests/unit/test_cad_material_line_runtime.py mold_cost_/tests/unit/test_cad_upload_runtime.py mold_cost_/tests/unit/test_cad_region_runtime.py mold_cost_/tests/unit/test_cad_prepare_runtime.py mold_cost_/tests/unit/test_cad_source_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py mold_cost_/tests/unit/test_cad_split_persistence_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py -q
19 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
180 passed


本轮继续往 CAD split 中心编排靠近了一步，把“子图识别 + 导出”这段 orchestration 也收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/cad_analysis_runtime.py`，把 `scripts/cad_chaitu/main.py` 里围绕 `analysis_system` 的那段稳定编排逻辑收出来了：包括读取 `analyze_cad_file_streaming()` 的结果、判断是否识别到子图、调用 `resolve_region_infos()`、再根据 `build_batch_export_list()` 和 `batch_export_regions_concurrent()` 组织导出，最后用 `collect_export_files()` 把导出结果归一化。这里我没有去动 analyzer/export 的底层实现，只把 orchestration ownership 收回到了 `src`。

这一步之后，CAD 方向剩余的 legacy 重点就更明确了。外围的来源解析、输入准备、编号编排、板料线、上传、持久化和子图识别/导出 orchestration 都已经有了 src-owned runtime；真正还没迁出的，主要是 `analysis_system`、`analyzer`、`MaterialLineIntegrator` 这些实现本体，以及 feature 侧的 `slider_red_face_updater`。也就是说，后面的活会更偏“算法实现迁移”，而不再是边界整理。

本轮验证结果：

pytest mold_cost_/tests/unit/test_cad_analysis_runtime.py mold_cost_/tests/unit/test_cad_material_line_runtime.py mold_cost_/tests/unit/test_cad_upload_runtime.py mold_cost_/tests/unit/test_cad_region_runtime.py mold_cost_/tests/unit/test_cad_prepare_runtime.py mold_cost_/tests/unit/test_cad_source_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py mold_cost_/tests/unit/test_cad_split_persistence_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py -q
21 passed

pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q
182 passed
本轮继续把 CAD split 的 `.x_t` 导出实现本体也往 `src` 收了一步。

这次主要扩展了 `src/mold_cost/infrastructure/cad/cad_xt_export_runtime.py`：除了保留原来的“检查 NX 环境、下载 PRT、组织导出目录”编排外，现在连总装组件索引、`part_code` 匹配、ParasolidExporter 导出以及 MinIO 上传这段实现也已经直接落在 `src`。`scripts/cad_chaitu/main.py` 不再自己维护 `_export_xt_from_prt()` 的 NX 导出细节，而是直接复用新的 src-owned 导出函数。

这样一来，CAD 侧 `.x_t` 这条链就从“只有准备逻辑在 src”进一步变成了“准备 + 组件匹配 + 导出实现都在 src”。R6 剩余的重点也因此更收敛了，后面主要还是 `analysis_system` / `analyzer` / `MaterialLineIntegrator` 这些算法本体和 `chaitu_process` 主流程 glue。

本轮验证结果：

`pytest mold_cost_/tests/unit/test_cad_xt_export_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py -q`

结果：`7 passed`

`pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q`

结果：`186 passed`
本轮继续把 `scripts/cad_chaitu/main.py` 中段那段“分析 -> 板料线 -> 上传 -> `.x_t` -> 落库”的主流程编排也收进了 `src`。

这次新增了 `src/mold_cost/infrastructure/cad/cad_process_runtime.py`，把拆图主链里最稳定的一段 orchestration 做成了独立 runtime：先解析来源并准备 DXF，再串联 `cad_analysis_runtime`、`cad_material_line_runtime`、`cad_upload_runtime`、`cad_xt_export_runtime` 和 `cad_split_persistence_runtime`，最后统一清理分析缓存与临时目录。与此同时，`scripts/cad_chaitu/main.py` 的 `chaitu_process()` 现在只保留 legacy 管理器初始化、入口参数准备和最终 API 返回格式，不再自己持有中段处理细节。

这一步的意义比较大，因为 R6 里最显眼的一个“主流程 still in legacy”块已经明显缩小了。接下来 CAD 方向剩余的重心就更清楚了，主要是 `analysis_system` / `analyzer` / `MaterialLineIntegrator` 这些算法实现本体，以及少量入口初始化胶水，而不再是整段编排都压在 `main.py` 里。

本轮验证结果：

`pytest mold_cost_/tests/unit/test_cad_process_runtime.py mold_cost_/tests/unit/test_cad_xt_export_runtime.py mold_cost_/tests/unit/test_cad_split_refactor.py -q`

结果：`9 passed`

`pytest mold_cost_/tests/unit mold_cost_/tests/integration mold_cost_/tests/golden -q`

结果：`188 passed`
