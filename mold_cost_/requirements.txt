# =============================================================================
# 合并信息
# 合并日期: 2026-02-10
# 源文件: mold_cost-main/requirements.txt + mold_cost_/requirements.txt
# 合并策略: 合并两个版本的依赖，使用较新的版本号
# =============================================================================

# ========== Web框架 ==========
fastapi==0.128.0  # 使用 mold_cost-main 的较新版本
uvicorn[standard]==0.40.0  # 使用 mold_cost-main 的较新版本
python-multipart==0.0.21  # 使用 mold_cost-main 的较新版本
websocket-client==1.9.0  # 来自 mold_cost-main
websockets==16.0  # 来自 mold_cost-main

# ========== 数据库 ==========
sqlalchemy[asyncio]==2.0.45  # 使用 mold_cost-main 的较新版本
asyncpg==0.30.0  # 使用 mold_cost-main 的较新版本
alembic==1.18.0  # 使用 mold_cost-main 的较新版本
psycopg2-binary==2.9.11  # 来自 mold_cost-main

# ========== 消息队列 ==========
aio-pika==9.5.7  # 使用 mold_cost-main 的较新版本

# ========== 对象存储 ==========
minio==7.2.20  # 使用 mold_cost-main 的较新版本

# ========== 认证和安全 ==========
python-jose[cryptography]==3.3.0  # 来自 mold_cost-main
PyJWT==2.10.1  # 来自 mold_cost_（JWT标准库），与 mcp 依赖兼容
passlib[bcrypt]==1.7.4  # 两者相同
cryptography==42.0.0  # 来自 mold_cost_

# ========== 缓存 ==========
redis[hiredis]==7.1.0  # 使用 mold_cost-main 的较新版本（包含 hiredis 扩展）

# ========== HTTP客户端 ==========
httpx==0.28.1  # 使用 mold_cost-main 的较新版本
aiohttp==3.13.3  # 使用 mold_cost-main 的较新版本
requests>=2.31.0  # 来自 mold_cost-main（用于 MCP 客户端）

# ========== 配置管理 ==========
python-dotenv==1.2.1  # 使用 mold_cost-main 的较新版本
pydantic==2.12.5  # 使用 mold_cost-main 的较新版本
pydantic-settings==2.12.0  # 使用 mold_cost-main 的较新版本

# ========== 日志 ==========
python-json-logger==4.0.0  # 使用 mold_cost-main 的较新版本
loguru>=0.7.0  # 来自 mold_cost-main

# ========== AI框架（Agent编排）==========
langchain==1.2.3  # 使用 mold_cost-main 的明确版本
langgraph==1.0.5  # 使用 mold_cost-main 的明确版本
langchain-openai==1.1.11  # 与当前 langchain/langchain-core 版本兼容
openai==2.26.0  # 满足 langchain-openai 的 OpenAI SDK 依赖

# ========== MCP Protocol ==========
mcp>=1.25,<2  # 来自 mold_cost-main

# ========== 工具库 ==========
tenacity==9.1.2  # 来自 mold_cost-main（重试机制）
python-dateutil==2.8.2  # 来自 mold_cost_

# ========== Excel & PDF ==========
openpyxl==3.1.5  # 来自 mold_cost-main
xlsxwriter==3.2.0  # 来自 mold_cost-main
reportlab==4.4.7  # 来自 mold_cost-main

# ========== CAD 文件处理 ==========
ezdxf>=1.0.0  # 来自 mold_cost-main

# ========== 测试 ==========
pytest==9.0.2  # 使用 mold_cost-main 的较新版本
pytest-asyncio==0.21.1  # 使用 mold_cost-main 的版本
pytest-cov==4.1.0  # 两者相同

# ========== 代码质量 ==========
black==25.12.0  # 使用 mold_cost-main 的较新版本
flake8==6.1.0  # 使用 mold_cost-main 的较新版本
mypy==1.19.1  # 使用 mold_cost-main 的较新版本

# ========== 监控 ==========
prometheus-client==0.23.1  # 来自 mold_cost-main
